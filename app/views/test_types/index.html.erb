<h1>Performance Tests By Type</h1>

<%= button_to "Add New Test Type", { action: "new"}, method: :get %>

<% @test_types.each do |type| %>
    <p><%= link_to type.name, test_type_path(type) %></p>
<% end %>